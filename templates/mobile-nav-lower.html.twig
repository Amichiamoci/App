<nav class="lower border-top w-100 ms-0 me-0 position-fixed bottom-0">
    <div class="row">
        <div class="col p-1 d-flex jusity-content-center align-items-center">
            <div>
                1
            </div>
        </div>
        <div class="col p-1 d-flex jusity-content-center align-items-center">
            <a href="{{ path('leaderboard') }}" 
                class="d-flex align-items-center text-decoration-none text-reset"
                title="Classifica corrente"
            >
                <twig:ux:icon name="iconoir:leaderboard-star" height="16" />
            </a>
        </div>
        <div class="col p-1 d-flex jusity-content-center align-items-center">
            <div>
                3
            </div>
        </div>
        <div class="col p-1 d-flex jusity-content-center align-items-center">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('profile') }}" 
                    class="d-flex align-items-center text-decoration-none text-reset"
                    title="{{ app.user.getName() }}"
                >
                    <twig:ux:icon name="uil:user" height="16" />
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" 
                    class="d-flex align-items-center text-decoration-none text-reset"
                    title="Accedi"
                >
                    <twig:ux:icon name="material-symbols:login" height="16" />
                </a>
            {% endif %}
        </div>
    </div>
</nav>